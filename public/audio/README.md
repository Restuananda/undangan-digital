# Background Music

Place a single MP3 file here named `wedding.mp3`. It's referenced by
`wedding.music.src` in `src/data/wedding.ts` and controlled by the
floating music button once the invitation is opened.

Keep it under ~5MB and make sure you have the rights to use the track.
